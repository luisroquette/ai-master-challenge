# Phase 2a Research Evaluation

Artifact: `.claude/skills/explainable-crm-prioritization/SKILL.md`
Task: `.specs/tasks/draft/implement-explainable-lead-scorer.feature.md`
Date: 2026-09-21
Evaluator: independent researcher judge, using SDD 3.6.0 researcher role and judge methodology.

## Evidence and scope

Read the complete 133-line skill, relevant task requirements/reference, research scratchpad, repository README and challenge README. No CLAUDE.md or constitution.md was found in the solution. The referenced research scratchpad exists. Read-only `git diff --check` passed. This is documentation/research evaluation: implementation, installation and model execution were not performed and are not inferred from the result.

Independently inspected the official LogisticRegression API, CalibratedClassifierCV API and pinned calibration source. The current LogisticRegression signature and deprecation notice support `l1_ratio=0`; FrozenEstimator documentation supports calibrating the provided disjoint data without refitting the frozen model. URLs:

- https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LogisticRegression.html
- https://scikit-learn.org/stable/modules/generated/sklearn.calibration.CalibratedClassifierCV
- https://raw.githubusercontent.com/scikit-learn/scikit-learn/1.9.1/sklearn/calibration.py

No existing application toolchain exists yet. Missing implementation tests are therefore a later implementation gate, not a failed research artifact. No task or skill content was modified.

## Detailed evaluation

### Resource Coverage — weight 0.30

Evidence: skill lines 23–37 link official modeling, calibration, data joins, cache/state, testing, security and source references. Lines 39–57 compare sklearn alternatives, optional SHAP, pandas/stdlib and native Streamlit, with exact candidate pins and explicit runtime limits. Lines 116–121 link four applicable implementations; the referenced research scratchpad records 36 sources and maintenance metadata.

Assessment: coverage exceeds basic resource enumeration and supports the relevant decisions. However, most API links use mutable `stable` URLs while the examples target a fixed sklearn release; package constraints and runtime compatibility remain distinct and are correctly labeled. These are useful, evidence-based recommendations, not a reproduced dependency environment.

Score: **4/5**.

Improvement: use version-specific documentation links for the chosen sklearn release wherever available, alongside the existing pinned source links.

### Pattern Relevance — weight 0.25

Evidence: lines 61–88 provide auditable joins, row eligibility, fingerprinting, three temporal periods, route-specific calibration and suppression policy. Lines 90–108 distinguish actual output reconstruction, calibrated log-odds and relative evidence. Lines 110–114 separate shared immutable models from session overrides.

Assessment: patterns map directly to the task's hardest failure modes and include usable generic examples. The sparse-evidence section says to use a nested backoff hierarchy, but its concrete example remains only in the scratchpad; an implementer reading the skill alone must still invent the hierarchy shape. This is a modest actionability gap, not missing architecture.

Score: **4/5**.

Improvement: add one generic hierarchy example such as segment+channel → segment → global, explicitly identifying it as configurable domain policy.

### Issue Anticipation — weight 0.20

Evidence: lines 63–67 address null join matching, unknown categories, invalid rows and untrusted artifacts; lines 84–88 address one-class periods, fallback validity, historical feature availability, band reliability and selection optimism; lines 94–106 address attribution order, calibration sign reversal and double-counted support; lines 112–127 address stale selections, shared state, incomplete evidence and missing outcomes.

Assessment: broad, unusually concrete mitigation coverage. The remaining gap is the resolved-only sample's relationship to active opportunities: line 71 requires resolved outcomes and line 127 mentions selection bias generally, but the skill does not explicitly discuss delayed outcomes/right censoring or define the prediction horizon. A chronological split of resolved cases cannot itself establish performance on still-open opportunities.

Score: **4/5**.

Improvement: add a short limitation requiring an explicit target horizon/cohort interpretation and warning that unresolved opportunities can create censoring bias; recommend mature cohorts or future outcome collection when that cannot be assessed from the static extract.

### Reusability — weight 0.15

Evidence: lines 14–21 describe domain concepts without dataset IDs or challenge labels; lines 75–82 and 102–104 use generic examples. The document avoids the task's exact product alias, file counts, metrics and implementation module names. However, lines 65 and 112 phrase account-free routing and pin expiration as universal rules, although these are project policies.

Assessment: reusable across CRM prioritizers sharing this architecture, with minor policy coupling. This is appropriately narrower than a general ML handbook, but the distinction between methodological invariants and product choices could be clearer.

Score: **4/5**.

Improvement: label account-free fallback and reset-on-recalculation as policies for this class of prototype, while retaining the general constraints on leakage, validation and shared mutable state.

### Task Integration — weight 0.10

Evidence: task lines 95–99 place the mandatory skill instruction immediately under Description and give the exact existing skill path. The referenced skill has discoverable YAML name/description, and the task's existing requirements remain present.

Assessment: the requested integration is explicit, prominent and resolves correctly. No missing reference or ambiguous prerequisite was found.

Score: **5/5**.

## Weighted result

| Criterion | Score | Weight | Weighted |
|---|---:|---:|---:|
| Resource Coverage | 4 | 0.30 | 1.20 |
| Pattern Relevance | 4 | 0.25 | 1.00 |
| Issue Anticipation | 4 | 0.20 | 0.80 |
| Reusability | 4 | 0.15 | 0.60 |
| Task Integration | 5 | 0.10 | 0.50 |
| Total | | 1.00 | **4.10/5.00** |

Threshold: **3.50/5.00**. Result: **PASS**. Verdict: **GOOD**.

This passes the research/reusability gate only. It does not approve later probability claims, package execution, deployment or model performance.

## Self-verification

1. Did the rating depend on document length? No; scores cite concrete patterns, resource coverage and specific remaining gaps.
2. Was research mistaken for runtime validation? No; the skill's candidate-only compatibility language and unexecuted gates were retained.
3. Is the task reference actually present and resolvable? Yes, task lines 97–99 point to the inspected file.
4. Were core API recommendations independently checked? Yes, current official estimator and calibration docs support the cited usage; this was not a full audit of all 36 sources.
5. Are missing numeric thresholds a research failure? No; the skill correctly requires predeclaration, while the task/specification must choose values before implementation/evaluation.

Adjustments: none. Confidence: high for coverage, relevance and integration; medium for unexecuted package/model integration.

Operational note: the mandated scratchpad script resolved the repository root. Its empty generated fd28a1a9.md was removed and this report was written inside the authorized submission subtree instead. The shared root .gitignore edit predates that generated file and was left untouched.
